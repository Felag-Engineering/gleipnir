.env
gcp-oauth.keys.json
.claude/agent-memory/go-code-reviewer/MEMORY.md
.claude/worktrees/
devflow/.devflow/
frontend/node_modules
frontend/dist

# React Component Preview
previewConfig.json
*storybook.log
storybook-static
node_modules

# Playwright MCP artefacts (screenshots, snapshots, console logs)
.playwright-mcp/
*.png
*.jpeg
/gleipnirctl
/gleipnir-plugin

# Local plugin bundles extracted by the loader at runtime (owned by container root)
local-plugins/installed/
local-plugins/*.tar.gz
local-plugins/*.tgz
