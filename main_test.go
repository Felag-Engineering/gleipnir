package main

// Provider registry tests removed: providers are now bootstrapped from
// DB-stored encrypted API keys via the admin handler, not from env vars.
// The admin handler tests in internal/admin/ cover key storage and retrieval.
