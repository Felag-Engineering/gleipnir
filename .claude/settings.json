{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash(git commit*)",
        "hooks": [
          {
            "type": "command",
            "command": "go vet ./... && go build ./... && go test ./...",
            "timeout": 300,
            "statusMessage": "Running go vet, build, and tests..."
          },
          {
            "type": "command",
            "command": "if git diff --cached --name-only | grep -q '^frontend/'; then cd frontend && npm run build && npx vitest run; fi",
            "timeout": 180,
            "statusMessage": "Running frontend build + tests (if frontend files staged)..."
          }
        ]
      }
    ],
    "Stop": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "command": "gofmt -w -s . && rm -rf .dev-loop"
          }
        ]
      }
    ]
  }
}
