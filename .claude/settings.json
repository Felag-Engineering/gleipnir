{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash(git commit*)",
        "hooks": [
          {
            "type": "command",
            "command": "go vet ./... && go build ./... && go test ./...",
            "timeout": 300,
            "statusMessage": "Running go vet, build, and tests..."
          },
          {
            "type": "command",
            "command": "if git diff --cached --name-only | grep -q '^frontend/'; then cd frontend && npx vitest run; fi",
            "timeout": 120,
            "statusMessage": "Running frontend tests (if frontend files staged)..."
          }
        ]
      }
    ],
    "Stop": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "command": "gofmt -w -s . && rm -rf .dev-loop"
          }
        ]
      }
    ]
  }
}
