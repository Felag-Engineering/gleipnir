/ntfy

# Signing material — NEVER commit. Keys are the plugin author's, kept out of
# the repo (generated via `gleipnir-plugin keygen`).
*.key
*.pub
*.minisig

# Packaged bundles are build output (`gleipnir-plugin package`).
*.tar.gz
*.tgz
